package com.example.medvisionsort.theme

import androidx.compose.ui.graphics.Color

// Premium MedVisionSort Core Palette
val DeepNavy = Color(0xFFF8FAFC)      // Soft gray/white page background (--bg-primary)
val SlateDark = Color(0xFFFFFFFF)     // Pure white card background (--bg-secondary)
val BgTertiary = Color(0xFFF1F5F9)    // Tertiary background (--bg-tertiary)

// Clinical Branding & Gradient Colors
val PrimaryTeal = Color(0xFF0284C7)   // Medical Blue (--primary)
val PrimaryTealLight = Color(0xFF0EA5E9) // Neon Blue (--primary-neon)
val SecondaryIndigo = Color(0xFF06B6D4) // Cyan Accent (--accent)
val SecondaryIndigoLight = Color(0xFF22D3EE) // Cyan Accent Light (--accent-light)

// Modality & Status Specific Colors (Vibrant Clinical Palette)
val MriIndigo = Color(0xFFC084FC)     // MRI Light Purple
val CtOrange = Color(0xFF06B6D4)      // CT Cyan Glow
val XrayTeal = Color(0xFFFBBF24)      // X-Ray Amber
val UnclassifiedRed = Color(0xFFEF4444) // Error/Danger/Pending Red

// Neutral Text & Borders
val TextPrimary = Color(0xFF0F172A)   // Deep Slate Primary Text
val TextSecondary = Color(0xFF475569) // Slate Muted Secondary Text
val TextMuted = Color(0xFF94A3B8)     // Muted/Disabled Grey Text
val BorderGlass = Color(0xFFE2E8F0)   // Soft Border Line

